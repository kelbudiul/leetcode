class Solution:
    def mirrorReflection(self, p: int, q: int) -> int:
        gcd = math.gcd(p, q)
        p //= gcd
        q //= gcd
        
        if p % 2 == 0:
            return 2
        elif q % 2 == 0:
            return 0
        else:
            return 1