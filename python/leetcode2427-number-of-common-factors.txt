class Solution:
    def commonFactors(self, a: int, b: int) -> int:
        counter = 0

        for j in range(1, min(a, b) + 1):
            if (a % j == 0) and (b % j == 0):
                counter += 1
        return counter